# 設計判断ログ

決定事項と未決事項を時系列で記録する。

---

## 2026-07-17 初回ヒアリング・方針決定

### 技術方針
- **採用**: Google スプレッドシート + GAS + Google Calendar API
- **不採用**: Webアプリ(Next.js等) → この規模ではオーバーエンジニアリング
- **不採用**: Notion + Zapier → 月額コスト・複雑化
- **保留**: Beds24 API連携 → 経営者の許可が取れれば将来的に利用。当面はExcel手動インポートで運用

### ヒアリング回答（確定済み）

1. **Googleカレンダー移行** → 可能。スタッフ合意済み。
2. **出勤可否の入力粒度** → 現状は○×の2値。新ルールの作成もOK。
3. **Rクリーンへの依頼方法** → スタッフ同様カレンダーで依頼。依頼・連絡の条件もスタッフと同様に扱ってよい。
4. **割り当ての自動反映** → 管理者承認ステップは不要。自動反映してよい。

5. **Beds24のExcel出力頻度** → 現在の週1回のままでよい。
6. **ユニット間の移動時間・エリア制約** → 移動時間は考慮しなくてよい。
7. **管理者の主な作業端末** → PC。スプレッドシートのUIはPC向けで設計する。
8. **「未割り当て」ユニットの扱い** → 後から確定して再出力される前提で無視してよい。
9. **今後のスタッフ増員見込み** → 一旦増員はないものとする。現在の2名+Rクリーン体制で設計。

### ヒアリング完了

全9項目の回答が揃った。実装に必要な情報は出揃っている。

### 設計上の確定事項まとめ（v1時点）

- **入力**: Beds24 Excelを週1回手動でスプレッドシートに貼り付け
- **スタッフ予定**: Googleカレンダーに○×で入力（TimeTreeから移行）
- **マッチング**: GASで自動実行、管理者承認不要で直接カレンダー反映
- **外注**: スタッフで対応不可 → Rクリーンに自動割り当て（カレンダー経由で依頼）
- **UI**: PC向けスプレッドシート
- **割り当てロジック**: 移動時間考慮なし、1ユニット=1人、当日清掃が原則
- **スタッフ構成**: 細田さん・普久原さんの2名固定 + Rクリーン

---

## 2026-07-20 仕様変更: 割り当てロジック＋カレンダー入力方式

### 変更の背景
- 調査の結果、普久原さんは全曜日対応可能と判明（細田さんは変わらず月火木日）
- 1日の処理上限もスタッフが都度カレンダーで申告する方式に変更
- 経営者から優先割り当て＋均等化の要望あり

### 変更内容

#### 1. カレンダー入力方式の変更
- **旧**: ×(不可) を入力。入力なし＝出勤可
- **新**: 数字0〜9を入力（その日の対応可能件数）。入力なし＝デフォルト上限（スタッフは0=不可、Rクリーンは99=常時可）
- フェイルセーフ: 入力忘れ→出勤不可扱いとなり、誤って割り当てられることを防ぐ
- 対応フォーマット: 半角(0-9)・全角(０-９)・丸付き数字(⓪-⑨)

#### 2. 曜日固定テーブルの廃止
- **旧**: 設定シートに「曜日別処理上限」テーブル（月=細田3件、水=普久原3件...）
- **新**: 固定テーブル廃止。処理可能件数はカレンダーの数字入力で都度決定

#### 3. 優先割り当て＋均等化ロジック
- **優先ルール**: 月火木日は細田さんに2件まで優先的に割り当て
- **均等化ルール**: 4件以上 かつ 両者出勤 → 処理件数を均等に分配（例: 4件→2-2、5件→3-2）
- **非優先曜日(水金土)**: 普久原さんを先に割り当て
- **Rクリーン**: どちらも対応不可な分を割り当て（従来通り）

### 設計上の確定事項まとめ（v2時点）

- **入力**: Beds24 Excelを週1回手動でスプレッドシートに貼り付け（変更なし）
- **スタッフ予定**: Googleカレンダーに **数字(0〜9)** で対応可能件数を入力
- **マッチング**: 優先割り当て(2件) → 均等化(4件以上) → Rクリーン(残り)
- **外注**: スタッフで対応不可 → Rクリーンに自動割り当て（変更なし）
- **UI**: PC向けスプレッドシート（変更なし）
- **スタッフ出勤日**: 細田さん=月火木日 / 普久原さん=全曜日 / Rクリーン=常時

---

## 2026-07-20 仕様変更: 差分同期・API最適化・新入力フォーマット

### 変更の背景
- Google Calendar APIの読み書き量が多く、レート制限に引っかかる
- 予約の入れ替わりを検知できない（予約IDがなかった）
- 経営者が手動でカレンダーの割り振りを変更するケースがある
- 顧客の特殊要望（消毒ポット、ベビーベッド等）をカレンダーに表示したい

### 変更内容

#### 1. 入力データ形式の変更
- **旧**: チェックアウト日, ユニット, ゲスト, 泊数
- **新**: 予約ID, タイトル, チェックアウト日, ユニット
- 予約IDで個別の予約を一意に識別し、差分検出の基盤とする
- タイトルで顧客の特殊要望を保持

#### 2. 同期データベースシート（新規追加）
- スプレッドシート内に「同期データベース」シートを追加
- 前回同期時の状態（予約ID・担当・カレンダーイベントID等）を保管
- 次回実行時にDBと新データを比較して差分のみ処理

#### 3. カレンダーAPI呼び出しの最適化
- **書き込み**: 毎回全削除→全作成 → 差分のみ作成・削除（大幅削減）
- **読み込み**: 全日付範囲 → 新規予約がある日付のみ
- 変更のないイベントには一切触れない

#### 4. 手動カレンダー編集の保持
- 経営者が手動でカレンダーを書き換えた場合、次回実行で上書きしない
- DBの差分のみ更新するため、手動変更は自然に保持される

#### 5. カレンダー出力にタイトルを含む
- タイトルがある予約: `担当⇒unit(タイトル)` 例: `細田さん⇒b1(消毒ポット)`
- タイトルなしの予約: `担当⇒unit` 例: `普久原さん⇒c4`

### 設計上の確定事項まとめ（v3時点）

- **入力**: Beds24 Excelを週1回手動でスプレッドシートに貼り付け（形式変更: 予約ID付き4列）
- **スタッフ予定**: Googleカレンダーに数字(0〜9)で対応可能件数を入力（変更なし）
- **マッチング**: 既存割り当て保持 + 新規分のみ割り当て（変更なし → Rクリーン → 未割当）
- **カレンダー同期**: 差分のみ書き込み（API呼び出し最小化）
- **手動編集**: 経営者のカレンダー手動変更は保持
- **UI**: PC向けスプレッドシート（変更なし）+ 同期データベースシート追加

---

## 2026-07-20 仕様変更: 翌日清掃ルール追加

### 変更の背景
- タイトルに「翌日清掃」を含む予約がRクリーン（外注）に回ると不要なコストが発生する
- 翌日にスタッフが対応可能であれば、翌日に清掃を回す方が経済合理性が高い
- 翌日に宿泊予定がないユニットであれば、翌日清掃でも運営上支障がない

### 変更内容

#### 1. 翌日清掃ルール（新規追加）
- **対象**: タイトルに「翌日清掃」を含む予約
- **ロジック**: 2フェーズ方式
  - フェーズ1: 通常の割り当てアルゴリズム（優先→均等化→Rクリーン→未割当）を実行
  - フェーズ2: Rクリーンまたは未割当になった「翌日清掃」対象を、翌日のスタッフに振り替え
- **原則**: 当日スタッフに余裕があれば当日に割り当て。翌日送りは外注回避のための最後の手段
- **ステータス**: 翌日に回った予約は `確定（翌日）` として表示

#### 2. チェックアウト日と清掃日の分離
- **旧(v3)**: チェックアウト日＝清掃日（常に同日）
- **新(v4)**: チェックアウト日と清掃日を別管理。翌日清掃の場合、清掃日＝チェックアウト日+1
- DBに「清掃日」列を追加（8列構成）。v3の7列DBも後方互換で読み込み可能

#### 3. カレンダー表示の変更
- カレンダーイベントの日付は「清掃日」を使用
- 翌日清掃の予約はチェックアウト翌日のカレンダーに表示される

### 設計上の確定事項まとめ（v4時点）

- **入力**: Beds24 Excelを週1回手動でスプレッドシートに貼り付け（変更なし）
- **スタッフ予定**: Googleカレンダーに数字(0〜9)で対応可能件数を入力（変更なし）
- **マッチング**: フェーズ1(通常割り当て) → フェーズ2(翌日清掃の振り替え)
- **翌日清掃**: 「翌日清掃」タイトル付き予約は外注回避のため翌日スタッフに優先的に振り替え
- **カレンダー同期**: 差分のみ書き込み（変更なし）。清掃日ベースでイベント日付を決定
- **手動編集**: 経営者のカレンダー手動変更は保持（変更なし）
- **UI**: 割り当て結果に「清掃日」列追加、翌日送り件数を完了メッセージに表示

---

## 2026-07-22 仕様変更: 入力形式変更・翌日清掃ロジック刷新・害虫防止ルール

### 変更の背景
- タイトルに「翌日清掃」と記載する方式では、直前に予約が入った場合に翌日清掃が間に合わない
- 実際の予約データ（開始日・チェックアウト日）から清掃可能期間を自動判定する方が安全で正確
- ゴミを放置すると害虫が発生するため、清掃はチェックアウトから2日以内に完了する必要がある

### 変更内容

#### 1. 入力データ形式の変更（4列→6列）
- **旧(v4)**: 予約ID, タイトル, チェックアウト日, ユニット
- **新(v5)**: 予約ID, タイトル, 開始日, チェックアウト日, ユニット, ゲスト
- 開始日を追加することで、同一ユニットの予約間ギャップを計算可能に
- ゲスト数を追加することで、Rクリーンのコスト最適化が可能に

#### 2. 翌日清掃判定の刷新
- **旧(v4)**: タイトルに「翌日清掃」を含むかで判定
- **新(v5)**: 同じユニットの次の予約の開始日を見て清掃期限を自動計算
  - 清掃期限 = min(次の予約の開始日 - 1日, チェックアウト日 + 2日)

#### 3. 害虫防止: チェックアウト+2日以内の清掃を保証
- **制約**: 清掃はチェックアウト日、翌日、翌々日のいずれかで完了しなければならない
- **優先順位**: スタッフで+2日以内に清掃 > Rクリーンで+2日以内に清掃 > 清掃未完了（禁止）
- 清掃は早いほうが良い（当日 > 翌日 > 翌々日）

#### 4. 4フェーズ割り当てアルゴリズム
- **Phase 1**: チェックアウト日当日にスタッフ割り当て（細田さん→普久原さん→未割当）
- **Phase 2**: 未割当について +1日・+2日でスタッフ枠を探索（早い日優先）
- **Phase 3**: 14日以内の未割当 → Rクリーン（安全ネット）
- **Phase 4**: Rクリーンコスト最適化（同日のスタッフ担当の中でゲスト数最少の部屋にRクリーンを入れ替え）

### 設計上の確定事項まとめ（v5時点）

- **入力**: Beds24 Excelを週1回手動でスプレッドシートに貼り付け（形式変更: 6列、開始日・ゲスト数追加）
- **スタッフ予定**: Googleカレンダーに数字(0〜9)で対応可能件数を入力（変更なし）
- **マッチング**: Phase 1(当日割り当て) → Phase 2(+1/+2日でスタッフ振り替え) → Phase 3(Rクリーン安全ネット) → Phase 4(Rクリーンコスト最適化)
- **清掃期限**: チェックアウト+2日が絶対上限（害虫防止）
- **カレンダー同期**: 差分のみ書き込み（変更なし）。清掃日ベースでイベント日付を決定
- **キャンセル検知**: DBにあるが予約データから消えた未来の予約 → カレンダーに「キャンセルされました」と表示
- **手動編集**: 経営者のカレンダー手動変更は保持（変更なし）

---

## 2026-07-23 機能追加: タイムラインシート

### 変更の背景
- 経営者から「翌日清掃の自動割り当ては信頼しているが、確認・チェックできるようにしておいてほしい」という要望
- 清掃延期がどのように発生しているか、視覚的に確認できる手段が必要

### 変更内容

#### 1. タイムラインシート（新規追加）
- 「タイムライン」シートにユニット×日付のガントチャートを自動生成
- 割り当て実行時に自動更新される
- 行: ユニット(b2〜c4)、列: 日付
- 色分け: 宿泊中(青)、CO日(オレンジ)、スタッフ清掃(緑)、Rクリーン(紫)、未割当(赤)、清掃猶予(黄)
- 清掃可能期間（`…`マーク）で延期の判断根拠が視覚的にわかる

### 設計上の確定事項まとめ（v6時点）

- **入力**: Beds24 Excelを週1回手動でスプレッドシートに貼り付け（変更なし）
- **スタッフ予定**: Googleカレンダーに数字(0〜9)で対応可能件数を入力（変更なし）
- **マッチング**: Phase 1〜4（変更なし）
- **清掃期限**: チェックアウト+2日が絶対上限（変更なし）
- **カレンダー同期**: 差分のみ書き込み（変更なし）
- **キャンセル検知**: 変更なし
- **手動編集**: 変更なし
- **タイムライン**: ユニット×日付のガントチャートで清掃延期状況を視覚的に確認可能（新規）

---

## 2026-07-23 機能追加: Beds24 API連携・週次自動実行

### 変更の背景
- 毎週のExcel手動エクスポート→スプシ手動貼り付け→手動GAS実行のワークフローを自動化したい
- 経営者からBeds24 APIアクセスの許可を取得済み
- GASの時間ベーストリガーは無料で利用可能

### 変更内容

#### 1. Beds24 API V2連携
- 招待コード → リフレッシュトークン → アクセストークンの認証フロー
- `GET /bookings` で予約データを自動取得（ページネーション対応）
- `GET /properties` でユニットマッピング（roomId → b2, s1等）を自動取得
- API応答を既存の6列入力形式に自動変換

#### 2. 全自動実行
- `runAllAuto()`: Beds24取得 → 割り当て → タイムライン → カレンダー反映を一括
- トリガー実行時はUI不要（Logger出力のみ）

#### 3. 週次トリガー
- 設定シートの曜日・時刻に基づく時間ベーストリガー
- デフォルト: 毎週月曜6時

### 設計上の確定事項まとめ（v7時点）

- **入力**: Beds24 API V2から自動取得（手動貼り付けも引き続き可能）
- **スタッフ予定**: Googleカレンダーに数字(0〜9)で対応可能件数を入力（変更なし）
- **マッチング**: Phase 1〜4（変更なし）
- **清掃期限**: チェックアウト+2日が絶対上限（変更なし）
- **カレンダー同期**: 差分のみ書き込み（変更なし）
- **キャンセル検知**: 変更なし
- **手動編集**: 変更なし
- **タイムライン**: 変更なし
- **自動化**: Beds24→スプシ→割り当て→カレンダーの全フロー自動化（新規）
- **トリガー**: 週次自動実行（無料、GAS時間ベーストリガー）（新規）

---

## 2026-07-27〜08-07 運用調整（v7 稼働後の修正）

実運用に入ってから判明した点を、都度コードに反映した記録。

### 1. 週次トリガー → 日次トリガー（2026-07-27）
- 週1回では直前の予約変更に追随できないため、毎日実行に変更
- 設定シートの「自動実行曜日」を廃止し、「自動実行時刻」（B17、初期値6時）のみに簡素化
- 副次効果: 毎日APIを呼ぶため、Beds24のリフレッシュトークン（30日間未使用で失効）が失効しなくなった

### 2. ユニットマッピングを roomId:unitId の組で判定（2026-07-27）
- 1つの roomId に複数の unit がぶら下がる構成があり、roomId だけでは特定できなかった
- 設定シートのマッピング表を「roomId・unitId・ユニット名・備考」の4列に変更
- roomId の確認用に「🔍 Beds24 roomId確認」メニューを追加

### 3. タイトル項目の取得元を修正（2026-07-27）
- Beds24 APIのメモ／コメント欄ではなく、`guestTitle` / `title` フィールドを参照するよう修正

### 4. 差分検出からタイトルを除外（2026-07-31）
- タイトルの表記ゆれだけでカレンダーイベントが不要に再作成される問題を修正
- 差分判定の対象は「チェックアウト日・清掃日・ユニット・担当」のみとする

### 5. カレンダーイベント名の変更（2026-07-31）
- **旧**: `細田さん⇒b4(消毒ポット)`
- **新**: `細田⇒b4(消毒ポット)次3人`
- 「さん」を落として短縮し、次の宿泊者の人数を追加（清掃時の準備数の目安になるため）

### 6. Rクリーン割り当て期限は14日のまま維持（2026-07-31 / 08-07）
- 一度8日に短縮したが、経営者の要望により14日に戻した（2度の差し戻しを経て確定）

### 7. 福田さんを第3優先スタッフとして追加（2026-08-07）
- スタッフ2名体制 → 3名体制に変更（v1時点の「増員なし」の前提が変わった）
- 優先順位: ① 細田さん → ② 普久原さん → ③ 福田さん → ④ 未割当 → ⑤ Rクリーン（14日以内）
- 設定シートのレイアウトを簡素化（スタッフ3名+Rクリーンを A3:C6 に集約、出力カレンダーIDは B12）
- v2で導入した曜日別の優先割り当て・均等化ルールは廃止（カレンダーの数字申告のみで判断）

### 8. Phase 1.5: 既存の未割当の再割り当て（2026-08-07）
- 一度「未割当」になった予約が、後からスタッフの空き枠ができても放置される問題を修正
- Phase 1 の後に、既存の未割当をスタッフの空き枠へ再割り当てするフェーズを追加

### 設計上の確定事項まとめ（現行）

- **入力**: Beds24 API V2から自動取得（手動貼り付けも可能。6列形式）
- **スタッフ**: 細田さん・普久原さん・福田さんの3名 + Rクリーン
- **スタッフ予定**: Googleカレンダーに数字(0〜9)で対応可能件数を入力（未入力=出勤不可）
- **マッチング**: Phase 1 → 1.5 → 2 → 2.5 → 3（Rクリーン14日以内） → 4（コスト最適化）
- **清掃期限**: min(次の予約の開始日 - 1日, チェックアウト日 + 2日)
- **カレンダー同期**: 差分のみ書き込み。手動編集は保持
- **トリガー**: 日次自動実行（既定 6時）

---

## 2026-09-04 機能追加: 稼働監視・異常通知（v8）

### 変更の背景

管理者の不在中にBeds24のリフレッシュトークンが失効し、清掃予定の自動反映が
停止していたが、誰も気づけなかった。構造的な原因は2つ。

1. `runAllAuto()` が全てのエラーを握りつぶして `Logger.log` に書くだけだったため、
   Apps Scriptからは「正常終了」に見え、Googleの実行失敗通知メールも飛ばなかった
2. リフレッシュトークンは30日間使われないと失効するが、日次トリガーが止まった
   時点では何も通知されず、失効するまで放置された

「自動化されているものは、壊れたことが分かる仕組みまで含めて自動化する」必要がある、
というのが今回の学び。

### 変更内容

#### 1. 異常時のメール通知
- スクリプト実行者宛にメール通知する `notifyOwner_()` を追加（12時間に1回まで）
- 通知対象: トークン失効 / 接続エラー / 実行エラー / 取得0件 / 未割当あり /
  3日以上正常終了なし / トークン20日間未使用

#### 2. キープアライブトリガー
- 全自動実行とは**別のトリガー**（12時間ずらし）で `beds24KeepAlive()` を日次実行
- リフレッシュトークンを能動的に使って失効を防ぎ、あわせて稼働状態を監視する
- 全自動実行が壊れても見張り役が生き残る、という二重化の考え方

#### 3. トークン失効の検知
- HTTP 401/403（失効・取り消し）と、それ以外（一時的障害）を区別
- 401/403 のときは設定シートを `要再接続 ✗` にし、復旧手順つきでメール通知

#### 4. 稼働状態の可視化
- メニューに「🩺 稼働状態を確認」を追加（トリガー・最終正常終了・トークン残日数・通知先）
- 設定シートに「最終自動実行」（A18/B18）を追加し、毎回の結果を記録

### 未解決の課題

- トリガー自体がGoogle側で無効化された場合、スクリプト内から検知する手段がない
  （トリガーが動かなければ通知コードも動かないため）。
  現状はトリガーを2つに分けて片方が生き残る確率を上げる対策にとどまる。
  完全な検知には外部からの死活監視が必要。
